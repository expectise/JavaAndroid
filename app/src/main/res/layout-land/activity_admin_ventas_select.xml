<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".activity.AdminVentasSelectActivity">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView
                android:id="@+id/nickname"
                style="@style/articuloRecycledLandscape"
                android:textColor="@color/red"
                android:text="Nick Name"/>

            <TextView
                android:id="@+id/costoTotal"
                style="@style/articuloRecycledLandscape"
                android:textColor="@color/red"
                android:text="Costo Total"/>

            <TextView
                android:id="@+id/costoEnvio"
                style="@style/articuloRecycledLandscape"
                android:textColor="@color/red"
                android:text="Costo Envío"/>
        </LinearLayout>


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/ventas_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/ventas_list_item"/>
    </LinearLayout>
</layout>