<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        tools:context=".activity.AdminPujaSelectActivity">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:orientation="horizontal">
            <TextView
                android:id="@+id/titulo"
                style="@style/articuloRecycledLandscape"
                android:textColor="@color/red"
                android:text="Título"/>

            <TextView
                android:id="@+id/fechaFinal"
                style="@style/articuloRecycledLandscape"
                android:textColor="@color/red"
                android:text="Fecha Final"/>

            <TextView
                android:id="@+id/fechaPriAbo"
                style="@style/articuloRecycledLandscape"
                android:textColor="@color/red"
                android:text="Fecha Primer Abono"/>
        </LinearLayout>


        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/pujas_recycler"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            tools:listitem="@layout/pujas_list_item"/>
    </LinearLayout>
</layout>